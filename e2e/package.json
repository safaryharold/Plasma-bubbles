{
  "name": "ibp-e2e",
  "version": "1.0.0",
  "description": "Playwright end-to-end tests for IBP Analytics Platform",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
